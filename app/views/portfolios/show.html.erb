<div class="container">
    <div class="row">
        <div class="col-md-7">
            <p><%= image_tag @portfolio_item.main_image %></p>
        </div>
        <div lass="col-md-5">
            <h3><%= @portfolio_item.title %></h3>
                <hr>
                    <p><%= @portfolio_item.body %></p>
                <hr>
                <h3>Technologies Used:</h3>
                <% @portfolio_item.technologies.each do |t|%>
                    <p><%= t.name %></p>
                <% end %>
        </div>
    </div>
</div>